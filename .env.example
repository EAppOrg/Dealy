# Database — matches docker-compose.yml (port 5433)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/dealy?schema=public"

# Auth (placeholder — wire real provider in a future batch)
AUTH_SECRET="change-me-in-production"

# App
NEXT_PUBLIC_APP_NAME="Dealy"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
